require 'spec_helper'

describe Omikujibashira do
  it 'has a version number' do
    expect(Omikujibashira::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
